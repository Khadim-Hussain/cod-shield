{
  "name": "cod-shield",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "shopify app dev",
    "build": "cd web && npm run build",
    "start": "node server.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@shopify/shopify-api": "^11.0.0",
    "@upstash/redis": "^1.34.0",
    "axios": "^1.6.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0"
  }
}
